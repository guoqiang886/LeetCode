给定一个字符串 s ，找到其中最长的回文子序列，并返回该序列的长度。可以假设 s 的最大长度为 1000 。

 

示例 1:
输入:

"bbbab"
输出:

4
一个可能的最长回文子序列为 "bbbb"。

示例 2:
输入:

"cbbd"
输出:

2
一个可能的最长回文子序列为 "bb"。

 

提示：

1 <= s.length <= 1000
s 只包含小写英文字母

-------------------------------------------------------Code-----------------------------------------------------------
class Solution {
    public int longestPalindromeSubseq(String s) {
        // 动态规划
        // dp[i][j]表示i和j之间子串的最长回文子序列长度
        // s[i] == s[j]，则dp[i][j] = dp[i + 1][j - 1] + 2，否则dp[i][j] = Math.max(dp[i + 1][j], dp[i][j - 1]);
        int n = s.length();
        int[][] dp = new int[n][n];
        for (int i = 0; i < n; i++) { // 注意初始条件是i = j的时候最长回文子序列是1
            dp[i][i] = 1;
        }
        for (int i = n - 2; i >= 0; i--) {
            for (int j = i + 1; j < n; j++) { // 注意dp[i][j]，如果i > j是没有意义的，所以只需要求i < j的部分即可，且遍历需要逆序遍历
                if (s.charAt(i) == s.charAt(j)) {
                    dp[i][j] = dp[i + 1][j - 1] + 2;
                } else {
                    dp[i][j] = Math.max(dp[i + 1][j], dp[i][j - 1]);
                }
            }
        }
        return dp[0][n - 1];
    }
}
