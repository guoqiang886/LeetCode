实现 int sqrt(int x) 函数。

计算并返回 x 的平方根，其中 x 是非负整数。

由于返回类型是整数，结果只保留整数的部分，小数部分将被舍去。

示例 1:

输入: 4
输出: 2
示例 2:

输入: 8
输出: 2
说明: 8 的平方根是 2.82842..., 
     由于返回类型是整数，小数部分将被舍去。
     
------------------------------------------------Code--------------------------------------------
class Solution {
    public int mySqrt(int x) {
        // 二分法
        // 如果mid * mid <= x < (mid + 1) * (mid + 1)，则mid为x的整数平方根
        if (x == 0) return 0;
        
        int low = 1;
        int high = x;
        while (low <= high) {
            int mid = (low + high) / 2;
            // 注意这里有一个技巧，如果比较x < mid * mid则会存在int数值越界
            // 所以这里比较x / mid < mid，避免了越界的情况出现
            if (x / mid < mid) {
                high = mid - 1;
            } else if (x / (mid + 1) >= (mid + 1)) {
                low = mid + 1;
            } else {
                return mid;
            }
        }
        return -1;
    }
}
